# MathMate 改动清单

> 分支：`feature/ability-radar-chart` → `main`  
> 总变更：12 个文件，+2727 行，-308 行

---

## 新增文件（8 个）

| 文件 | 说明 |
|------|------|
| `lib/models/library_question.dart` | 题库题目数据模型（映射服务器 API 字段） |
| `lib/models/user_radar_profile.dart` | 六维能力画像模型 + `dimensionTags` 标签映射表 |
| `lib/pages/ability_assessment_page.dart` | 能力自评页面（五档滑动选择） |
| `lib/pages/practice_page.dart` | **练习页面** — 六维雷达图 + 服务器题目推荐 + 自由组卷 |
| `lib/pages/question_solver_page.dart` | **答题页面** — 单选/填空作答 + 自动评分 + 解析展示 |
| `lib/profile_radar_chart.dart` | 六边形雷达图 Canvas 绘制组件（动画展开） |
| `lib/services/ability_score_service.dart` | 能力评分服务（三阶算法：贝叶斯收缩 + 题量权重 + 难度修正） |
| `lib/services/library_question_service.dart` | **题库 API 服务** — 接入 mathmate.top 服务器，基于画像推荐题目 |

---

## 修改文件（4 个）

| 文件 | 改动内容 |
|------|----------|
| `lib/main.dart` | 新增第 4 个 Tab「练习」（位于笔记和我的之间），导航索引调整 |
| `lib/profile_page.dart` | 集成六维雷达图展示 + Tab 点击触发动画 |
| `lib/grade_selection_page.dart` | 小幅调整 |
| `pubspec.lock` | 依赖版本锁定更新 |

---

## 核心功能

### 1. 练习 Tab（全新页面）

- 顶部：六维能力雷达图 + "根据你的学习画像，为您推荐以下难度适配的题目："
- 左上角标注弱项维度及分数
- 题目列表：从服务器题库（`mathmate.top/api/library`，共 539 题）按能力画像推荐
- 「换一批」按钮：清空已展示记录，重新拉取
- 右下角悬浮「自由组卷」按钮（不随滚动移动）

### 2. 六维能力画像

| 维度 | 对应题库标签 | 题量 |
|------|-------------|------|
| 基础代数工具 | 集合与逻辑、复数、不等式 | 68 |
| 函数微积分 | 函数与导数 | 104 |
| 数列三角 | 数列、三角函数 | 112 |
| 平面与空间几何 | 向量、立体几何、解析几何 | 177 |
| 概率统计 | 概率统计 | 54 |
| 组合计数 | 计数原理 | 17 |

### 3. 推荐算法（四步）

1. **权重分配**：能力分越低 → 权重越大 → 多出题
2. **题目数分配**：每维度保底 1 题，剩余按权重分配
3. **难度区间**：弱项推简单题（0.15~0.35），强项推难题（0.45~0.80）
4. **取题排序**：从服务器按 section 拉取，按难度贴近度择优

### 4. 答题页面

- 单选题：点击选项卡片 → 提交 → 绿色/红色反馈 + 正确答案 + 详细解析
- 填空题：输入答案 → 提交 → 判断结果 + 解析
- 提交后自动记录到能力评分系统（正确率 + 题量 + 难度三维更新）
- 支持「重新作答」和「下一题」

### 5. 能力评分算法

三阶算法（所有 6 个维度统一）：

1. **贝叶斯收缩正确率**：自评分作为先验，虚拟样本量 n0=5
2. **题量熟练度权重**：指数增长曲线，半衰期 λ=30
3. **难度系数修正**：难度越高 → 分数加成越大

结果范围：1.0 ~ 5.0，展示时 ×2 映射为 10 分制。
